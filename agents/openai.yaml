interface:
  display_name: "Third-Party Image Gen"
  short_description: "Create and edit images through NewAPI providers"
  default_prompt: "Use $generation-image-for-api to create or edit a bitmap image through my current third-party provider."
policy:
  allow_implicit_invocation: true
